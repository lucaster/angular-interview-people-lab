export class FilteringService {
  /**
   * Keeps the elements which have length equal to @length.
   * The input elements array will not be changed.
   *
   * @param words: must not be modified
   * @param length: character length of words that will be kept
   * @return a new array
   */
  filterByLength(words: string[], length: number): string[] {
    // TODO
    return ['implement me!'];
  }

  /**
   * The classic Fizz Buzz ;)
   * Count from 1 to n:
   * Divisible by 3 --> print 'Fizz' to console
   * Divisible by 5 --> print 'Buzz' to console
   * Divisible by 3 and 5 --> print 'Fizz Buzz' to console
   */
  fizzBuzz(n: number): void {
    // TODO
  }
}
